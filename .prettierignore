# Agent Handoff owns exact managed blocks, installed artifacts, status, and tasks.
AGENTS.md
CLAUDE.md
.agents/agent-handoff/
.agents/skills/agent-handoff/
docs/STATUS.md
docs/TODO.md

# Mirrors the markdown-tooling `exclusions` in .standards/config.toml so a local
# run matches the format CI caller. Keep the two in sync.
docs/reference/pre-migration/
package-lock.json
products/vscode-extension/**/*.js
products/vscode-extension/**/*.mjs
products/vscode-extension/**/*.code-snippets

# Preserve local migration preview evidence outside tracked project content.
migration-plan.json
